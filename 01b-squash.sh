#! /usr/bin/env bash

echo "You've thought about it, and it'd actually be best if new-file is one commit."
echo "Use git rebase --interactive to squash everything into one commit."
echo "Then, after more thought, you've decided that BBB should really be a separate commit."
echo "Use git reset --patch to uncommit that one line and turn it into a new commit."
